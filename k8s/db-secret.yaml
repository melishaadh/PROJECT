# Placeholder values only — every `data:` entry below is base64 of an obvious
# stand-in (`CHANGE_ME_...`), not a real credential. Regenerate before any
# real deploy:
#   echo -n 'mongodb://mongo:27017/trekeasy' | base64
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" | base64
# For an actual cluster, prefer `kubectl create secret generic db-secret
# --from-literal=...` (or a sealed-secrets / external-secrets pipeline) over
# committing this file with real values filled in.
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: myapp
type: Opaque
data:
  MONGODB_URI: bW9uZ29kYjovL21vbmdvOjI3MDE3L3RyZWtlYXN5
  JWT_SECRET: Q0hBTkdFX01FXzMyX0NIQVJfTUlOSU1VTV9SQU5ET01fU0VDUkVU
  JWT_REFRESH_SECRET: Q0hBTkdFX01FXzMyX0NIQVJfTUlOSU1VTV9SRUZSRVNIX1NFQ1JFVA==
